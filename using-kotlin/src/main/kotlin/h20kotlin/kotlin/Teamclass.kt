package h20kotlin.kotlin
/*

class Team(val name: String) {
    private val members: MutableSet<String> = mutableSetOf()
    private val relationships: MutableList<Relationship> = mutableListOf()

    override fun toString(): String {
        return (listOf("In $name team made of ${members.sorted()}")
                + relationships).joinToString("\n")
    }
}

*/